import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "1337x | Ankit Singh",
  description:
    "The 1337X Link Converter is a tool designed to simplify the process of converting non-downloadable links from the 1337X website into direct, downloadable links. Created by Ankit Singh, this project aims to enhance user convenience by bypassing the complexities of accessing download links on torrent platforms",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
